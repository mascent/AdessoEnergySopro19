pipeline{
    agent any

    tools{nodejs "node"}

    stages{
        stage('configureInstall') {
            steps {
                sh 'cd web'
                sh 'npm ci'
            }
        }
        stage('test') {
            steps {
                sh 'cd web'
                sh 'npm test:coverage'
            }
        }
    }
}