import React from 'react';
import { storiesOf } from '@storybook/react';
import AdminDashBoard from './admin-dashboard';

storiesOf('Screens | Admin Dashboard', module).add('Default', () => (
  <AdminDashBoard />
));
